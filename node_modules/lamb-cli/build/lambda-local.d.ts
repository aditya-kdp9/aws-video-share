export declare class LambdaLocal {
    private config;
    constructor(config: {
        projectRoot: string;
    });
    createEntryFile({ dir, filename, typescript, }: {
        dir?: string;
        filename: string;
        typescript: boolean;
    }): Promise<string>;
    getZipUnit8Array({ entryFile, handlerFileName, }: {
        entryFile: string;
        handlerFileName?: string;
    }): Promise<Uint8Array>;
}
