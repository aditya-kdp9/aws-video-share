import { LambdaClient, LambdaClientConfig } from "@aws-sdk/client-lambda";
declare class LambdaExecution {
    protected config: LambdaClientConfig;
    protected client: LambdaClient;
    constructor(config: LambdaClientConfig);
}
export declare class CreateExecution extends LambdaExecution {
    execute({ name, unit8ArrayBuffer, }: {
        name: string;
        unit8ArrayBuffer: Uint8Array;
    }): Promise<{
        FunctionArn: string | undefined;
        RoleArn: string | undefined;
        RoleName: string | undefined;
    }>;
}
export declare class DeleteExecution extends LambdaExecution {
    execute({ FunctionArn, RoleName, }: {
        FunctionArn: string;
        RoleName: string;
    }): Promise<void>;
}
export {};
