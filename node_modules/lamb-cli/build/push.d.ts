/// <reference types="node" />
import { LambdaClientConfig } from "@aws-sdk/client-lambda";
export declare function push({ region, functionArn, zipBuffer, credentials, }: {
    zipBuffer: Buffer;
    functionArn: string;
} & LambdaClientConfig): Promise<import("@aws-sdk/client-lambda").UpdateFunctionCodeCommandOutput>;
