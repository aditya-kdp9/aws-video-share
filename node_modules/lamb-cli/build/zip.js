"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.createZipBuffer = void 0;
const adm_zip_1 = __importDefault(require("adm-zip"));
const esbuild_1 = __importDefault(require("esbuild"));
function createZipBuffer({ entry, config = {}, }) {
    return __awaiter(this, void 0, void 0, function* () {
        const { outputFiles, errors } = yield esbuild_1.default.build(Object.assign({ entryPoints: [entry], bundle: true, write: false, minify: true, platform: "node" }, config));
        if (errors.length > 0) {
            console.log(errors);
            throw new Error("Something went wrong in the build phase");
        }
        const zip = new adm_zip_1.default();
        zip.addFile(config.outfile || "index.js", Buffer.from(outputFiles[0].contents));
        return zip.toBuffer();
    });
}
exports.createZipBuffer = createZipBuffer;
